#require test.js
   //var dia, pos, bak, run, next, pause=false, info={speed:1,lines:0,score:0};
function start() { /*command
test*/
var aaa=1;/*djaoijdiasdjio*/ var hhh=2;
	output("只是测试");
output("只是测试");
}

#define function fun